const request = require('request');
const test = require('./test');

console.log(test);

// console.log('Going to make a request');

// request('http://google.com', (err, response, body) => {
//   if(err) throw err;
//   console.log('Got this response from Google: ', body);
// })